import React from "react";
import { useLocation, useParams } from "react-router-dom";

const Two = ({setShow}) => {

	
    return (
        <div>
			<h1>Two</h1>
        </div>
    );
};

export default Two;
